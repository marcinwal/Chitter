<!DOCTYPE html>
<html>
  <header>
    <div><a id="logo" href="/">Chitter</a></div>
    <div id="user-peeps">
      <% if current_user %>
        <span id="welcome-message">
           Welcome, <%= current_user.username.capitalize %>  <a href='/newpeep' id='newpeep' class='button'>Peep</a>
        </span>

        <form id="sign-out" method="post" action="/signout">
          <input type="hidden" name="_method" value="delete">
          <input type="submit" value="Sign out">
        </form>
      <% else %>
        Hello Chitter!
        <a href='/newuser' id='sign-up' class='button'>Sign up</a>
        <a href='/signin' id='sign-in' class='button'>Sign in</a> 
      <% end %>
    </div>  
  </header>
</html> 
